<h2>New membership</h2>

<%= render "form.html", changeset: @changeset,
                        action: membership_type_path(@conn, :create)%>

<%= link "Back", to: membership_type_path(@conn, :index) %>
