<h2>New resource</h2>

<%= render "form.html", changeset: @changeset,
                        action: admin_resource_path(@conn, :create) %>

<%= link "Back", to: admin_resource_path(@conn, :index) %>
