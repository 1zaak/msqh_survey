<h2>New membership</h2>

<%= render "form.html", changeset: @changeset,
                        action: membership_category_path(@conn, :create)%>

<%= link "Back", to: membership_category_path(@conn, :index) %>
