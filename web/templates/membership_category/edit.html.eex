<h2>Edit membership</h2>

<%= render "form.html", changeset: @changeset,
                        action: membership_category_path(@conn, :update, @membership_category) %>

<%= link "Back", to: membership_category_path(@conn, :index) %>
